import styled from 'styled-components'

const ContainerProductsData = styled.div`
  height: 640px;
  width: calc(100% - 85px);
  display: flex;
  flex-direction: row;
  margin: 0px 55px 30px 30px;
`;

export default ContainerProductsData
